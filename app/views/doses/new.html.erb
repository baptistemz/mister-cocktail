<div class="container">
  <%= simple_form_for ([@cocktail, @dose]) do|f| %>
    <%= f.association :ingredient %>
    <%= f.input :description, placeholder: "Poor 5cl" %>

    <%= f.submit %>
  <%end%>
</div>
